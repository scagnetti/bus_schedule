<div class="row">
  <div class="col-lg-2 col-md-4">

    <%= form_for(@bus_stop) do |f| %>
      <% if @bus_stop.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@bus_stop.errors.count, "error") %> prohibited this bus_stop from being saved:</h2>
          <ul>
            <% @bus_stop.errors.full_messages.each do |message| %>
              <li><%= message %></li>
            <% end %>
          </ul>
        </div>
      <% end %>

      <div class="form-group">
        <%= f.label :display_name %>
        <%= f.text_field :display_name, class: "form-control" %>
      </div>
      <div class="form-group">
        <%= f.label :search_name %>
        <%= f.text_field :search_name, class: "form-control" %>
      </div>
      <div class="form-group">
        <%= f.label :direction_id %>
        <%= f.select(:direction_id, options_from_collection_for_select(Direction.all, "id", "display_name"), {}, {class: "form-control"}) %>
      </div>
      <div class="actions">
        <%= f.submit nil, class: "btn btn-default"%>
      </div>
    <% end %>

  </div>
</div>