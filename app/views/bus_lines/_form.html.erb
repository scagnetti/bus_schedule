<%= form_for(@bus_line) do |f| %>
  <% if @bus_line.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bus_line.errors.count, "error") %> prohibited this line from being saved:</h2>

      <ul>
      <% @bus_line.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :name %>
    <%= f.text_field :name, class: "form-control" %>
  </div>

  <div class="actions">
    <%= f.submit nil, class: "btn btn-default" %>
  </div>
<% end %>

